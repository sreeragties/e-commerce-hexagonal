Installation Guide
docker compose up -d
docker compose exec node-angular npm install -g @angular/cli
docker compose exec node-angular npm install -g npm@11.3.0

docker compose exec node-angular npm install

docker compose exec node-angular ng serve --host 0.0.0.0